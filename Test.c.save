#include <stdio.h>
#include <unistd.h>
#include <stdint.h>
#include "omni.h"


int main( void)
{
	initController();
	while(1)
	{
		motorSpeed(MOTOR_2, 60);
		sleep(1);
		motorSpeed(MOTOR_2, 0);
		sleep(5);
	}
	return 0;
}
